# 贡献指南

感谢关注 `AI IDE Init Template`。本项目接受对工具适配、中文文档、安全边界和离线验证的改进。

## 贡献原则

- 配置格式必须来自目标工具的官方文档、官方内置说明或可复现的本机验证结果。
- 不得提交访问令牌、密码、Cookie、私钥、真实连接串、生产资源地址或个人敏感数据。
- 不得将未经证实的工具能力写成默认已支持功能。
- 新增外部服务时，默认采用非生产、只读优先和执行前审批原则。
- 面向使用者的说明和模板指令使用中文；工具要求的键名、标识符和命令保留原格式。

## 提交流程

1. 从 `main` 创建功能分支。
2. 保持修改集中于一个清晰主题，例如新增一个工具模板或完善一个验证点。
3. 补充或更新相关初始化指南。
4. 在仓库根目录执行：

```bash
python3 scripts/verify_all_templates.py
```

5. 在 Pull Request 中说明修改目的、采用的官方依据、验证结果以及需要人工确认的运行时步骤。

## 新增工具模板要求

新增 AI IDE 模板应至少包含：

- 项目规则或说明入口。
- 可验证的流程 Skills 或等价能力。
- 中文初始化指南。
- 不依赖真实凭据的离线验证脚本。
- 明确的 MCP、外部写入、支付、发布和生产访问安全边界。

如果目标工具没有公开且稳定的项目级配置格式，应提供保守模板与人工配置说明，不猜测目录或配置字段。
