---
description: 前端、UI、UX、视觉和可访问性任务规则。
alwaysApply: false
enabled: true
globs:
  - "src/**/*.{ts,tsx,js,jsx,vue,svelte,css,scss,html}"
  - "app/**/*.{ts,tsx,js,jsx,css,scss}"
---

# 前端与体验规则

- UI、UX、视觉和交互任务开始前，先使用 `experience-specification` 明确目标用户、核心路径、状态、空态、错误态和可访问性要求。
- 新增组件或页面时，遵循项目已有设计系统、组件库、布局密度、命名方式和状态管理模式。
- 表单、列表、导航、弹窗、异步加载、错误提示和权限不足状态必须有明确表现。
- 响应式布局至少说明桌面和移动端的关键断点、内容优先级和文本溢出处理。
- 交付前给出可验证的前端检查项：类型检查、Lint、单测、组件测试、端到端测试或人工验收路径。
