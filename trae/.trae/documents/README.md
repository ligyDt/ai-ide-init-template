# TRAE Plan 工作流目录

TRAE SOLO 的 `/plan` 工作流会在本目录下生成 `plan.md`，用于中小型功能开发和模块级重构。

建议在以下场景使用 `/plan`：

- 范围明确的功能实现。
- 局部重构、文档整理或测试补齐。
- 不需要完整 Spec 文档组，但需要先对齐执行步骤和验收标准。

执行前先确认计划内容；若计划涉及外部写入、支付、部署、生产访问或真实数据，必须先完成安全审查。
