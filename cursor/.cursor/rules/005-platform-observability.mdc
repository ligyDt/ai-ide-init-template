---
description: 平台、成本、Token、延迟、质量、CI/CD、监控与本地降级规则
alwaysApply: true
---

# 平台与可观测

- 涉及发布、部署、CI/CD、监控、告警、成本、Token、延迟、质量或模型路由时，先明确环境、权限、预算、SLO 和回滚方式。
- 默认只规划和验证非生产环境；生产发布、基础设施变更、镜像推送、支付和真实用户数据处理必须单独确认。
- 性能优化必须先建立基线指标，再提出可回滚的小步改动。
- CI/CD 应优先纳入 lint、类型检查、测试、安全扫描、许可证检查和构建验证。
- 离线、本地或断网模式下，应明确哪些能力不可用、哪些验证可以本地完成、哪些需要恢复网络或 IDE 授权后人工确认。
